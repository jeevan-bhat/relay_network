services:
  - type: web
    name: terminal-relay
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 10000
      - key: DB_PATH
        value: /app/data/relay.db
